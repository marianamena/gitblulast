1. with root: systemctl restart postgresql@16-main.service

2.with user postgres: write psql ,then with \c <databasename> like \c boc ,then:

CREATE EXTENSION pgaudit;
then check per database:

SELECT * FROM pg_extension WHERE extname = 'pgaudit';

3.select pg_reload_conf(); :run per database





1.58 bludesk
81.82 metabaselog
72.6 password manager
91.24 sentry
apt search postgresql-16-pgaudit
apt search postgresql-12-pgaudit
apt download postgresql-16-pgaudit >> to check if the package is download or not :cd /etc/apt,inja bayad bashe package k download shode.

check dependency in package:apt-cache showpkg postgresql-15-pgaudit
apt install --download-only -y pkgname

1.apt-get intsall postgresql-13-pgaudit (postgres version 13.11) first check with : apt download postgresql-12-pgaudit (after sysops said repository is added) then in apt install package --assume-no packagename
apt download postgresql-15-pgaudit
check with apt install --assum-no ./postgres-12-pgadut...
2. backup from postgres.conf and hba
3. vim /etc/postgresql/main/postgres.conf
add shared_preload_libraries = 'pgaudit'
restart postgres :systemctl restart postgres@13-main.service
4.psql : CREATE EXTENSION pgaudit; \c to database: and do it per database:


5.check with psql: SELECT * FROM pg_extension WHERE extname = 'pgaudit'; check it per database
6.
alter system set log_destination = 'csvlog';
alter system set logging_collector = 'on'; in restart mikhad
alter system set log_filename='postgresql-%Y-%m-%d_%H%M%S.log';
alter system set log_connections = 'on';
alter system set log_rotation_age='7d'; badan check konam baraye linux hande
alter system set log_rotation_size =0; in restart mikhad
ALTER SYSTEM SET pgaudit.log TO 'ddl,  role'; in ham restart nemikhad

7.psql select pg_reload_conf();
taghirati k ba alter system zade mishavad dar file postgreSQL.auto.conf mineshnad
restart postgres
##############################################


--========check and install pgaudit  ( sysops bayad repository ro mount kone)

apt search pgaudit
apt-get install postgresql-XX-pgaudit  # replace XX with your PostgreSQL version

1.apt

======Add extention to config file


1. Edit postgresql.conf: Locate the postgresql.conf file, usually found in the PostgreSQL data directory. 

cat /var/lib/pgsql/12/data/postgresql.conf | grep shared_preload_libraries

---As the root user, add or modify the following configuration settings: 

shared_preload_libraries = 'pgaudit'



2. Restart PostgreSQL: Restart the PostgreSQL service to apply the changes

systemctl restart PostgreSQL 

3. Create the extension in the database

a:  Connect to your PostgreSQL instance:

psql

b: Create the extension in the desired database:

CREATE EXTENSION pgaudit;

4. Verify installation

SELECT * FROM pg_extension WHERE extname = 'pgaudit';

############################################################

------more pgaudit configuration :

alter system set log_destination = 'csvlog';
alter system set logging_collector = 'on'; in restart mikhad
alter system set log_directory='/pg_log/log';  ----> create folder on every server ,in ro masir ro check kon age nabashe k besazesh va check kardan permission ha
alter system set log_filename='postgresql-%Y-%m-%d_%H%M%S.log';
alter system set log_connections = 'on';
alter system set log_rotation_age='7d';
alter system set log_rotation_size =0; in restart mikhad
ALTER SYSTEM SET pgaudit.log TO 'ddl, role'; in ham restart mikhad


select pg_reload_conf();


--restart postgresql


SELECT pg_is_wal_replay_paused();  stop & SELECT pg_wal_replay_resume(); start

UserParameter=postgres.checkpgisinrecovery, cat /tmp/check_pg_is_recovery
