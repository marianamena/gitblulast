create standby:



*primary:
1.check archive mode:
if not: 
        shutdown immediate;
		startup mount;
		alter database archivelog;
		alter database open;
2.check force logging in database:
        select force_logging from v$database;
    if it is no :
	    alter database force logging;


*in primary and gurad:
    1.check ping between each other
	2.add primary and guard in both /etc/hosts
	
*in primary and standby ,check sid and unique name :(sid should be the same)
    select name from v$database; to check sid 
	show parameter unique name ,you should know the unique name in datagurad.
	
* in primary :
     ALTER SYSTEM SET LOG_ARCHIVE_CONFIG='DG_CONFIG=(ccdb,ccdg)'; ccdb is unique name for primary,ccdg is for datanadby
	 
	 
* in primary 
    add standby log file : size of these should be same as primary log file( select * from v$logfile)
	number=( number of log file in primary)+1
	
Alter database add standby logfile group 6 size 512m;
Alter database add standby logfile group 7 size 512m;
Alter database add standby logfile group 8 size 512m;
Alter database add standby logfile group 9 size 512m;
Alter database add standby logfile group 10 size 512m;
Alter database add standby logfile group 11 size 512m;



   check it :
        SELECT group#, type, member FROM v$logfile  WHERE type = 'STANDBY';
		
		
* in primary:
    
    locate archive log for standby:
       
	   before it ,check : show parameter log_archive_dest_, to check maybe we have another guard:
	   first guard ,config log_archive_dest_2,second guard, config log_archive_dest_3
       ALTER SYSTEM SET LOG_ARCHIVE_DEST_2='service=ccdg lgwr sync affirm valid_for=(online_logfiles,primary_role) db_unique_name=ccdg';

	   
* in primary:
    check log_archive_dest_1  in primary , it is ok or not.
     Alter system set log_archive_dest_1='LOCATION=/fra/CCDB/archivelog/ VALID_FOR=(ALL_LOGFILES,ALL_ROLES) DB_UNIQUE_NAME=ccdb';
    or
    Alter system set log_archive_dest_1='LOCATION=+FRA01 VALID_FOR=(ALL_LOGFILES,ALL_ROLES) DB_UNIQUE_NAME=jira';
    or
    Alter system set log_archive_dest_1='LOCATION=USE_DB_RECOVERY_FILE_DEST VALID_FOR=(ALL_LOGFILES,ALL_ROLES) DB_UNIQUE_NAME=jira';
	
		
		
*in primary :
    check  LOG_ARCHIVE_MAX_PROCESSES , 30 is good: ALTER SYSTEM SET LOG_ARCHIVE_MAX_PROCESSES=30;

* in primary :
    check REMOTE_LOGIN_PASSWORDFILE,it should be exlusive 
    if not :
            ALTER SYSTEM SET REMOTE_LOGIN_PASSWORDFILE=EXCLUSIVE SCOPE=SPFILE;


* in primary :
   Alter system set fal_server='ccdg'; hafez abad should be fal-server


* in primary:
  Alter system set standby_file_management=auto;


* in primary: listener configuration:
  
  check database services in listener
  oracle_home for user oracle ,not grid!
  
  
* in primary ,tns setting:

 * in standby ,tns setting.
 * create directory in standby in os.
 
 * in primary scp spfile to standby
 edit it in standby:
 
 
 
 *.audit_file_dest='/u02/app/oracle/admin/ccdg/adump'
*.audit_trail='db'
*.compatible='12.2.0'
*.control_files='+DATA01/CCDB/CONTROLFILE/current.269.1076576613','/backup/fra/CCDG/controlfile/o1_mf_jfp3mw7r_.ctl'#Restore Controlfile
*.db_block_size=8192
*.db_create_file_dest='+DATA01'
*.db_file_name_convert='FRA01','+DATA01','OLOG01','+DATA01','OLOG02','+DATA01'
*.db_name='ccdb'
*.db_unique_name='ccdg'
*.db_recovery_file_dest='/backup/fra'
*.db_recovery_file_dest_size=500g
*.diagnostic_dest='/u02/app/oracle'
*.dispatchers='(PROTOCOL=TCP) (SERVICE=ccdgXDB)'
*.enable_pluggable_database=true
*.local_listener='LISTENER_CCDG'
*.log_archive_config='DG_CONFIG=(ccdg,ccdb)'
*.log_archive_format='%t_%s_%r.dbf'
*.log_file_name_convert='+FRA01','+DATA01'
*.nls_language='AMERICAN'
*.nls_territory='AMERICA'
*.open_cursors=300
*.pga_aggregate_target=3209m
*.processes=640
*.remote_login_passwordfile='EXCLUSIVE'
*.sga_target=9627m
*.standby_file_management='AUTO'
*.undo_tablespace='UNDOTBS1'
[oracle@VTM-STD ~]$ cat aminkhan.ora |grep convert
*.db_file_name_convert='FRA01','+DATA01','OLOG01','+DATA01','OLOG02','+DATA01'
*.log_file_name_convert='+FRA01','+DATA01'

.log_archive_config='DG_CONFIG=(ccdb,ccdg)'
 *.enable_pluggable_database=true 
 
 
 
* in standby :
instance ro havasemon bashe
shutdown immediate;
 startup nomount pfile='/home/oracle/init.ora';


*. in standby:

 create spfile='+DATA01/CCDG/PARAMETERFILE/spfile.ora' from pfile='/home/oracle/init.ora';
 
 with grid:
 srvctl modify database -db ccdg -spfile '+DATA01/ccdg/PARAMETERFILE/spfile.ora'
srvctl config database -db ccdg


* in standby:
shutdown immediate
startup nomount;



* in standby 
cd $ORACLE_HOME/dbs/
rm passwordfile
scp passwordfile from primary.

hatman mv kon esme password file ro:



*.standby :
with grid:
 srvctl modify database -db ccdg -pwfile /u02/app/oracle/product/12.2.0/dbhome_1/dbs/orapccdena
 
 
 
 
 *IN PRIMARY:
 sqlplus / as sysdba
 check: which LOG_ARCHIVE_DEST_STATE_2 is related to this standby:
 
 
  ALTER SYSTEM SET LOG_ARCHIVE_DEST_STATE_2=ENABLE;




in primary:
 rman target sys@almdb auxiliary sys@almdg
 DUPLICATE TARGET DATABASE FOR STANDBY FROM ACTIVE DATABASE DORECOVER nofilenamecheck ;


  
  
  
 * standby:
 shutdown immediate;
 startup nomount;
 
alter database open ;


start mrp;
ALTER DATABASE RECOVER MANAGED STANDBY DATABASE DISCONNECT;


check mro:
 
SQL> set pagesize 500;
SQL> set linesize 120;
SQL> select PROCESS,STATUS,SEQUENCE#,block# from v$managed_standby;






ALTER DATABASE RECOVER MANAGED STANDBY DATABASE DISCONNECT;
